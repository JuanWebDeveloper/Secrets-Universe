import { Fragment } from 'react';
import { WelcomeBanner } from '@/ui/components';

const HomePage = () => {
 return (
  <Fragment>
   <WelcomeBanner />
  </Fragment>
 );
};

export default HomePage;
